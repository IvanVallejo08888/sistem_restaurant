# Contraseña del Administrador (acceso al panel Admin)
NEXT_PUBLIC_ADMIN_PASSWORD=admin123

# Nombre del negocio
NEXT_PUBLIC_BUSINESS_NAME=Heladería Antojos

# --- Backend real (Fase de producción) ---
# Cambia a "true" y completa src/services/httpService.ts para usar API + base de datos.
# NEXT_PUBLIC_USE_BACKEND=false

# Conexión a Neon PostgreSQL (usada por las API Routes, no expuesta al cliente)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Secreto para autenticación futura
# AUTH_SECRET=
